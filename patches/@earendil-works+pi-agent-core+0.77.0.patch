diff --git a/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js b/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js
index 5b6becd..f079ab9 100644
--- a/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js
+++ b/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js
@@ -3,6 +3,26 @@
  * Transforms to Message[] only at the LLM call boundary.
  */
 import { EventStream, streamSimple, validateToolArguments, } from "@earendil-works/pi-ai";
+
+let _steeringInterrupt = false;
+let _pendingSteeringMessages = [];
+let _activeStreamAbort = null;
+let _partialOutput = "";
+export function injectSteeringMessage(message) {
+    _pendingSteeringMessages.push(message);
+    // If there's partial text output, inject a resume hint so the model
+    // continues from where it left off rather than restarting from scratch.
+    if (_partialOutput && _partialOutput.trim().length > 50) {
+        const _snippet = _partialOutput.slice(-200);
+        _pendingSteeringMessages.push({
+            role: "user",
+            content: [{ type: "text", text: `[System: You were mid-response when interrupted. Process the message above, then continue your response. Your response was cut off at: "...${_snippet}"]` }],
+            timestamp: Date.now(),
+        });
+    }
+    _steeringInterrupt = true;
+    _activeStreamAbort?.abort();
+}
 /**
  * Start an agent loop with a new prompt message.
  * The prompt is added to the context and events are emitted for it.
@@ -80,11 +100,26 @@ async function runLoop(initialContext, newMessages, initialConfig, signal, emit,
     let firstTurn = true;
     // Check for steering messages at start (user may have typed while waiting)
     let pendingMessages = (await config.getSteeringMessages?.()) || [];
+
+    _steeringInterrupt = false;
+    _pendingSteeringMessages = [];
+    _activeStreamAbort = null;
+    _partialOutput = "";
     // Outer loop: continues when queued follow-up messages arrive after agent would stop
     while (true) {
         let hasMoreToolCalls = true;
         // Inner loop: process tool calls and steering messages
-        while (hasMoreToolCalls || pendingMessages.length > 0) {
+        while (hasMoreToolCalls || pendingMessages.length > 0 || _steeringInterrupt) {
+            if (_steeringInterrupt) {
+                for (const message of _pendingSteeringMessages) {
+                    await emit({ type: "message_start", message });
+                    await emit({ type: "message_end", message });
+                    currentContext.messages.push(message);
+                    newMessages.push(message);
+                }
+                _steeringInterrupt = false;
+                _pendingSteeringMessages = [];
+            }
             if (!firstTurn) {
                 await emit({ type: "turn_start" });
             }
@@ -102,10 +137,17 @@ async function runLoop(initialContext, newMessages, initialConfig, signal, emit,
                 pendingMessages = [];
             }
             // Stream assistant response
+            _activeStreamAbort = new AbortController();
+            _partialOutput = "";
             const message = await streamAssistantResponse(currentContext, config, signal, emit, streamFn);
+            _activeStreamAbort = null;
             newMessages.push(message);
             if (message.stopReason === "error" || message.stopReason === "aborted") {
                 await emit({ type: "turn_end", message, toolResults: [] });
+                if (message.stopReason === "aborted" && _steeringInterrupt) {
+                    hasMoreToolCalls = false;
+                    continue;
+                }
                 await emit({ type: "agent_end", messages: newMessages });
                 return;
             }
@@ -114,7 +156,7 @@ async function runLoop(initialContext, newMessages, initialConfig, signal, emit,
             const toolResults = [];
             hasMoreToolCalls = false;
             if (toolCalls.length > 0) {
-                const executedToolBatch = await executeToolCalls(currentContext, message, config, signal, emit);
+                const executedToolBatch = await executeToolCalls(currentContext, message, config, signal, emit, newMessages);
                 toolResults.push(...executedToolBatch.messages);
                 hasMoreToolCalls = !executedToolBatch.terminate;
                 for (const result of toolResults) {
@@ -186,10 +228,13 @@ async function streamAssistantResponse(context, config, signal, emit, streamFn)
     const streamFunction = streamFn || streamSimple;
     // Resolve API key (important for expiring tokens)
     const resolvedApiKey = (config.getApiKey ? await config.getApiKey(config.model.provider) : undefined) || config.apiKey;
+    const llmSignal = _activeStreamAbort
+        ? AbortSignal.any([_activeStreamAbort.signal, ...(signal ? [signal] : [])])
+        : signal;
     const response = await streamFunction(config.model, llmContext, {
         ...config,
         apiKey: resolvedApiKey,
-        signal,
+        signal: llmSignal,
     });
     let partialMessage = null;
     let addedPartial = false;
@@ -213,6 +258,10 @@ async function streamAssistantResponse(context, config, signal, emit, streamFn)
                 if (partialMessage) {
                     partialMessage = event.partial;
                     context.messages[context.messages.length - 1] = partialMessage;
+                    if (event.type === "text_delta" || event.type === "text_end") {
+                        const _textParts = partialMessage.content.filter((c) => c.type === "text").map((c) => c.text);
+                        if (_textParts.length > 0) _partialOutput = _textParts.join("");
+                    }
                     await emit({
                         type: "message_update",
                         assistantMessageEvent: event,
@@ -251,18 +300,28 @@ async function streamAssistantResponse(context, config, signal, emit, streamFn)
 /**
  * Execute tool calls from an assistant message.
  */
-async function executeToolCalls(currentContext, assistantMessage, config, signal, emit) {
+async function executeToolCalls(currentContext, assistantMessage, config, signal, emit, newMessages) {
     const toolCalls = assistantMessage.content.filter((c) => c.type === "toolCall");
     const hasSequentialToolCall = toolCalls.some((tc) => currentContext.tools?.find((t) => t.name === tc.name)?.executionMode === "sequential");
     if (config.toolExecution === "sequential" || hasSequentialToolCall) {
-        return executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit);
+        return executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit, newMessages);
     }
-    return executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit);
+    return executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit, newMessages);
 }
-async function executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit) {
+async function executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit, newMessages) {
     const finalizedCalls = [];
     const messages = [];
     for (const toolCall of toolCalls) {
+        if (_steeringInterrupt) {
+            for (const message of _pendingSteeringMessages) {
+                await emit({ type: "message_start", message });
+                await emit({ type: "message_end", message });
+                currentContext.messages.push(message);
+                newMessages?.push(message);
+            }
+            _steeringInterrupt = false;
+            _pendingSteeringMessages = [];
+        }
         await emit({
             type: "tool_execution_start",
             toolCallId: toolCall.id,
@@ -296,9 +355,19 @@ async function executeToolCallsSequential(currentContext, assistantMessage, tool
         terminate: shouldTerminateToolBatch(finalizedCalls),
     };
 }
-async function executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit) {
+async function executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit, newMessages) {
     const finalizedCalls = [];
     for (const toolCall of toolCalls) {
+        if (_steeringInterrupt) {
+            for (const message of _pendingSteeringMessages) {
+                await emit({ type: "message_start", message });
+                await emit({ type: "message_end", message });
+                currentContext.messages.push(message);
+                newMessages?.push(message);
+            }
+            _steeringInterrupt = false;
+            _pendingSteeringMessages = [];
+        }
         await emit({
             type: "tool_execution_start",
             toolCallId: toolCall.id,
@@ -366,6 +435,13 @@ async function prepareToolCall(currentContext, assistantMessage, toolCall, confi
             isError: true,
         };
     }
+    if (signal?.aborted) {
+        return {
+            kind: "immediate",
+            result: createErrorToolResult("Operation aborted"),
+            isError: true,
+        };
+    }
     try {
         const preparedToolCall = prepareToolCallArguments(tool, toolCall);
         const validatedArgs = validateToolArguments(tool, preparedToolCall);
diff --git a/node_modules/@earendil-works/pi-agent-core/dist/agent.js b/node_modules/@earendil-works/pi-agent-core/dist/agent.js
index 4b1fae0..edd1c33 100644
--- a/node_modules/@earendil-works/pi-agent-core/dist/agent.js
+++ b/node_modules/@earendil-works/pi-agent-core/dist/agent.js
@@ -1,5 +1,5 @@
 import { streamSimple, } from "@earendil-works/pi-ai";
-import { runAgentLoop, runAgentLoopContinue } from "./agent-loop.js";
+import { runAgentLoop, runAgentLoopContinue, injectSteeringMessage } from "./agent-loop.js";
 function defaultConvertToLlm(messages) {
     return messages.filter((message) => message.role === "user" || message.role === "assistant" || message.role === "toolResult");
 }
@@ -165,7 +165,15 @@ export class Agent {
     }
     /** Queue a message to be injected after the current assistant turn finishes. */
     steer(message) {
-        this.steeringQueue.enqueue(message);
+        if (this.activeRun) {
+            // Agent is mid-run: use the interrupt path so the message is injected
+            // at the next tool boundary rather than waiting until end-of-turn.
+            injectSteeringMessage(message);
+        }
+        else {
+            // No active run: enqueue normally so it's picked up when the loop starts.
+            this.steeringQueue.enqueue(message);
+        }
     }
     /** Queue a message to run only after the agent would otherwise stop. */
     followUp(message) {
